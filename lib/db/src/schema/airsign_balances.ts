// airsign_balances table is deprecated and no longer used.
// The on-chain AirsignEscrow PDA replaced this off-chain record.
// Kept here to avoid a breaking DB migration; the underlying table is now orphaned.
